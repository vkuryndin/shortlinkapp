name: Runner smoke test

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: [self-hosted, dtrack]
    steps:
      - name: Who am I
        run: |
          set -eux
          hostname
          whoami
          pwd
          uname -a
          df -h
          docker --version || true
